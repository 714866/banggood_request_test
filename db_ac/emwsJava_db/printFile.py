# from mysql_connect import connect_dbfrom db_ac.modular.mapper import connect_DB# import pymysqlwms_db = "ewmsJava_mysql"selectSql = "select * from productshift where GoodsType=34"def findproductshiftBygoodstype(goodstype):    cursor = connect_DB(wms_db)    selectSql = "select productshiftcode from productshift where GoodsType=34"    newId=int(cursor.fetchone(selectSql)[0])    print(newId)    for i in range(1,9):        print(i)        if i <=4:            cursor.execute(inserSql.format(newId + i,delivery_type,0,processcenter))        else:            cursor.execute(inserSql.format(newId + i, delivery_type, 1, processcenter))    cursor.commitAndClose()if __name__=="__main__":    #设置处理中心    processcenter = 1000    #设置销售类型    delivery_type = 0    inserOutbountConfig(delivery_type,processcenter)